package com.tornyak;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativePasswordResourceIT extends PasswordResourceTest {

    // Execute the same tests but in native mode.
}